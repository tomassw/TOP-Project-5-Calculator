Probably going to make this out of divs

Needs the following functions: add substract multiply divide

Needs to work with floats

Should work both by adding the numbers using the buttons and by typing them at the keyboard